import { ConversationList } from './conversation-list.model';

describe('ConversationList', () => {
  it('should create an instance', () => {
    expect(new ConversationList()).toBeTruthy();
  });
});
